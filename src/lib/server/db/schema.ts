/**
 * Drizzle schema — Phase 0 stub.
 * Full schema (users, accounts, sessions, projects, services, devices, leads,
 * team_members, clients, timeline_milestones) is implemented in Phase 2.
 *
 * This file intentionally exports an empty object so `drizzle-kit generate`
 * succeeds against an empty migration in Phase 0.
 */

export {};
